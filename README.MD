# Lejiend Tutorial — Node + Express

## Setup

Install dependencies and start the server (the app serves a recruiter-focused portfolio site from `public/`):

```bash
npm install
npm start
```

Open `http://localhost:3000/` to view the site.
# nazher-sample-test
